int fib(int n) {
    int res = 0;
    if (n == 0 | n == 1) {
        res = 1;
    } else {
        res = fib(n - 1) + fib(n - 2);
    }

    return res;
}

main {
    int n = readInt();
    printInt(fib(n));
}